#include <iostream>
#include <fstream>
using namespace std;

void getInput (int * x, int * y)
{
    cout << "Enter the value of number 1: ";
    cin >> * x;
    cout << "Enter the value of number 2: ";
    cin >> * y;
}

int isSame